import React from 'react';
import Header from './Header';  // Ensure this matches exactly

const Home = () => {
  return (
    <div className='flex flex-col w-full h-auto items-center justify-center bg-primary'>

         <Header/>
             
    </div>
  );
};

export default Home;
